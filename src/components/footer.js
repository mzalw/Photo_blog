import React from "react";

const Footer = () => {
    return (
        <footer>
            <div className={"container"}>
                <div>
                    <a href={"."}>tag</a>
                    <a href={"."}> - tag</a>
                    <a href={"."}> - tag</a>
                    <a href={"."}> - tag</a>
                    <a href={"."}> - tag</a>
                </div>
            </div>
            <span>Copyright</span>
        </footer>
    );
}

export default Footer